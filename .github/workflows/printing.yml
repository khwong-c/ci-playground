name: Your CI Workflow

on: ["push"]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Fail it
        run: |
          exit -1
  job_tag:
    runs-on: ubuntu-latest
    if: ${{ startsWith(github.ref, 'refs/tags/') && success() }}
    needs: job1
    steps:
      - name: Tagged
        run: |
          echo I am tagged
